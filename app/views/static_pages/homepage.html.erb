<% unless user_signed_in? %>
  <div class="jumbotron text-center">
    <h1>file | FOLDER</h1>
    <p>Reporting and file storage solution for Ethiopia's forward thinking contractors!</p>
  </div>
  <div class="text-center">
    <%= render 'static_pages/plans' %>
  </div>
<% else %>
  <div class="panel panel-default text-center">
    <div class="panel-body">
      <h1>Welcome, <%= current_user.first_name%> <%= current_user.last_name %></h1>
      <button class="btn btn-lg btn-default"><%= link_to "View folders", folders_path %></button>
    </div>
  </div>

  <% if current_user.role == "admin" %>
    <!-- Show activity feed of users and documents -->
    <div class="well text-center">
      <h3>The activity feed goes here</h3>
    </div>
    <!-- Link to folders -->
  <% elsif current_user.role == "reporting" %>
    <!-- Show folder for the current_user's allowed folders -->
  <% elsif current_user.role == "standard" %>
    <!-- Not sure why this role even exists lol -->
  <% end %>
<% end %>