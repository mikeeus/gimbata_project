<li class="list-group-item">
  <span class="glyphicon glyphicon-edit"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong><%= activity.owner ? activity.owner.first_name : 'Guest' %></strong>
  <% if a.trackable %>
    edited the file <span class="activity-file-name"><%= a.trackable.name %></span>.
  <% else %>
    edited the file that is currently deleted.
  <% end %>
</li>