<li class="list-group-item">
  <span class="glyphicon glyphicon-plus"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong><%= activity.owner ? activity.owner.first_name : 'Guest' %></strong>
  <% if a.trackable %>
    added the file <span class="activity-file-name"><%= a.trackable.name %></span> to <span class="activity-folder-name"><%= a.trackable.folder.name %></span>.
  <% else %>
    added the file that is currently deleted.
  <% end %>
</li>